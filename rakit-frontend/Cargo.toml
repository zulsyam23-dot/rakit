[package]
name = "rakit-frontend"
version = "1.0.5"
edition = "2021"
description = "Rakit lexer and parser — Bahasa Indonesia keywords, JSX-like syntax"

[dependencies]
rakit-core = { path = "../rakit-core" }
rakit-ir-ast = { path = "../rakit-ir-ast" }
