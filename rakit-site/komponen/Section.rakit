// Komponen Section

komponen Section(judul: Teks, deskripsi: Teks) {
    tampilkan {
        <section className="section">
            <h2 className="section-title">{judul}</h2>
            {deskripsi != "" ? <p className="section-desc">{deskripsi}</p> : <div />}
        </section>
    }
}
