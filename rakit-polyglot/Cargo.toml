[package]
name = "rakit-polyglot"
version = "1.0.5"
edition = "2021"
description = "Rakit polyglot FFI — C, Python, Rust bindings"

[dependencies]
rakit-core = { path = "../rakit-core" }
rakit-ir-hir = { path = "../rakit-ir-hir" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
