[package]
name = "rakit-lsp"
version = "0.1.0"
edition = "2021"
description = "Rakit Language Server Protocol — full LSP implementation"

[dependencies]
rakit-core = { path = "../rakit-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
